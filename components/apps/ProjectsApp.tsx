"use client";

// Ventures are now shown as a dock folder popup (ProjectsFolder.tsx).
// This file is retained for compatibility but is no longer rendered.

interface Props {
  onClose: () => void;
  orientation: string;
}

export default function ProjectsApp(_props: Props) {
  return null;
}
